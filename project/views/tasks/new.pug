doctype html
html(lang="en")
    head
        title Document
    body
        form(action="/tasks" method="POST")
            textarea(name="description" placeholder="Introduce la tarea")
            input(type="submit" value="Guardar")