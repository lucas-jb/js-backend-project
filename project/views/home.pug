doctype html
html
    head
        title Bienvenido
    body
        h1= user.email
        form(action='/sessions?_method=DELETE' method="POST")
            input(type="submit" value="Cerrar sesión")
        script(src="/socket.io/socket.io.js")
        script. 
            socket = io.connect()
            socket.on('count_updated')